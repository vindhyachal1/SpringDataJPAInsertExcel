CREATE TABLE data (
--    id INT AUTO_INCREMENT PRIMARY KEY,
    co_id VARCHAR(255),
    phone_id VARCHAR(255),
    book_name VARCHAR(255),
    author_name VARCHAR(255),
    mobile_string VARCHAR(255),
    cst_date TIMESTAMP
);
